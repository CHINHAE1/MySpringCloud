spring:
  application:
    name: myh-product
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        prefix: myh-product
        file-extension: yml
        group: DEFAULT_GROUP
  profiles:
    active: dev 